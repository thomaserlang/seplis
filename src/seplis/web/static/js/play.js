(function($) {
    $.fn.seplis_play = function(url, play_id, options) {

        var settings = $.extend({
        }, options);

        var video = this;
 
        return this;
 
    };
 
}(jQuery));