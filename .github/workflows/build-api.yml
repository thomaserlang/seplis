name: build api

on:
  push:
    tags:
      - "api-v*"
  workflow_dispatch:

jobs:
  build-and-push-image:
    uses: ./.github/workflows/_docker-build.yml
    secrets: inherit
    with:
      image_name: ${{ github.repository }}-api
      context: .
      tag_prefix: api-v
